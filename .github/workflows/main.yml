name: Pull on Push

on:
  push:
    branches:
      - main

jobs:
  pull:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Pull changes
        run: git pull origin main
